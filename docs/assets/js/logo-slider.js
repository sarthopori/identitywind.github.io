document.addEventListener('DOMContentLoaded',()=>{const sliderContainer=document.querySelector('.logo-slider-container');if(!sliderContainer){return;}
const track=document.querySelector('.logo-slider-track');const slides=document.querySelectorAll('.logo-slide');const prevBtn=document.querySelector('.logo-slider-prev');const nextBtn=document.querySelector('.logo-slider-next');const realSlidesCount=slides.length/2;const slideWidth=slides[0].offsetWidth;const animationSpeed=30000;const transitionTime=0.5;track.style.width=`${slideWidth*slides.length}px`;let currentPosition=0;let slideInterval;function updatePosition(){track.style.transition=`transform ${transitionTime}s ease-in-out`;track.style.transform=`translateX(-${currentPosition*slideWidth}px)`;}
function nextSlide(){currentPosition++;if(currentPosition>=realSlidesCount){updatePosition();setTimeout(()=>{track.style.transition='none';currentPosition=0;track.style.transform=`translateX(0px)`;},transitionTime*1000);}else{updatePosition();}}
function prevSlide(){if(currentPosition<=0){track.style.transition='none';currentPosition=realSlidesCount;track.style.transform=`translateX(-${currentPosition*slideWidth}px)`;setTimeout(()=>{currentPosition--;updatePosition();},50);}else{currentPosition--;updatePosition();}}
nextBtn.addEventListener('click',()=>{nextSlide();clearInterval(slideInterval);slideInterval=setInterval(nextSlide,3000);});prevBtn.addEventListener('click',()=>{prevSlide();clearInterval(slideInterval);slideInterval=setInterval(nextSlide,3000);});sliderContainer.addEventListener('mouseenter',()=>clearInterval(slideInterval));sliderContainer.addEventListener('mouseleave',()=>{slideInterval=setInterval(nextSlide,3000);});slideInterval=setInterval(nextSlide,3000);});